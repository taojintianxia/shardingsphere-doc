commit ec4a896d7170ad417f073e310f1fd999889e4c08
Author: Liang Zhang <zhangliang@apache.org>
Date:   Mon Jun 29 18:22:20 2026 +0800

    Clarify ShardingSphere-MCP preview and HTTP safety guidance (#38949)
    
    * Clarify ShardingSphere-MCP preview and HTTP safety guidance
    
    - Add explicit HTTP bind/port/path defaults to the packaged MCP HTTP config
    - Clarify that built-in HTTP transport requires a trusted gateway for auth, rate limiting, and audit logging
    - Document that execute_update preview is classification-only and does not perform database-side trial execution
    - Add descriptor and prompt assertions for preview semantics
    
    * Clarify ShardingSphere-MCP preview and HTTP safety guidance
    
    - Add explicit HTTP bind/port/path defaults to the packaged MCP HTTP config
    - Clarify that built-in HTTP transport requires a trusted gateway for auth, rate limiting, and audit logging
    - Document that execute_update preview is classification-only and does not perform database-side trial execution
    - Add descriptor and prompt assertions for preview semantics

diff --git a/docs/document/content/reference/mcp/protocol-surface.cn.md b/docs/document/content/reference/mcp/protocol-surface.cn.md
index dc10e485f12..eaf769b0ff3 100644
--- a/docs/document/content/reference/mcp/protocol-surface.cn.md
+++ b/docs/document/content/reference/mcp/protocol-surface.cn.md
@@ -60,7 +60,7 @@ ShardingSphere-MCP 不要求 roots，也不会发送 `sampling/createMessage` 
 `database_gateway_execute_update`
 
 - 预览或执行一个支持的有副作用 SQL。
-- `execution_mode=preview` 只做分类和副作用范围预览。
+- `execution_mode=preview` 只做分类和副作用范围预览，不会在数据库侧进行试执行。
 - `execution_mode=execute` 在 review 后执行 SQL。
 - 多语句和禁用命令会被拒绝。
 
diff --git a/docs/document/content/reference/mcp/protocol-surface.en.md b/docs/document/content/reference/mcp/protocol-surface.en.md
index 6a5e98f4f34..7c17cf3440b 100644
--- a/docs/document/content/reference/mcp/protocol-surface.en.md
+++ b/docs/document/content/reference/mcp/protocol-surface.en.md
@@ -60,7 +60,7 @@ ShardingSphere-MCP does not require roots and does not send `sampling/createMess
 `database_gateway_execute_update`
 
 - Previews or executes one supported side-effecting SQL statement.
-- `execution_mode=preview` only classifies the SQL and previews the side-effect scope.
+- `execution_mode=preview` only classifies the SQL and previews the side-effect scope; it is not a database dry run.
 - `execution_mode=execute` executes the SQL after review.
 - Multiple statements and banned commands are rejected.
 
diff --git a/docs/document/content/user-manual/shardingsphere-mcp/deployment.cn.md b/docs/document/content/user-manual/shardingsphere-mcp/deployment.cn.md
index b43a6f4b803..33b08c6d095 100644
--- a/docs/document/content/user-manual/shardingsphere-mcp/deployment.cn.md
+++ b/docs/document/content/user-manual/shardingsphere-mcp/deployment.cn.md
@@ -77,12 +77,14 @@ docker run --rm -i \
 
 ## 安全部署建议
 
-内置 HTTP Server 不提供认证和授权。
+内置 HTTP Server 不提供认证、授权、限流或审计日志。
+不支持把内置 HTTP Server 直接暴露到公网作为生产部署边界。
 如果需要远程访问，应放在受信网络、反向代理或网关后面，由外层组件处理：
 
 - TLS 终止。
 - 身份认证。
 - 授权策略。
+- 限流。
 - 网络访问控制。
 - 审计日志。
 
@@ -90,7 +92,7 @@ HTTP 绑定建议：
 
 - 本地调试使用 `127.0.0.1`。
 - 容器或内网部署使用受控网络接口。
-- 面向远程客户端暴露时，避免直接裸露 MCP Server。
+- 面向远程客户端暴露时，避免直接裸露 MCP Server，应通过受信网关转发。
 - 需要把会话和外部用户或调用来源关联时，由受信网关注入会话归属请求头；不要允许客户端直接伪造这些请求头。
 
 ### 受信网关与 TLS 终止样例
diff --git a/docs/document/content/user-manual/shardingsphere-mcp/deployment.en.md b/docs/document/content/user-manual/shardingsphere-mcp/deployment.en.md
index bb3cd313596..f8ca8d11dc2 100644
--- a/docs/document/content/user-manual/shardingsphere-mcp/deployment.en.md
+++ b/docs/document/content/user-manual/shardingsphere-mcp/deployment.en.md
@@ -77,12 +77,14 @@ Configure `runtimeDatabases` according to the target capability boundary:
 
 ## Secure deployment
 
-The built-in HTTP Server does not provide authentication or authorization.
+The built-in HTTP Server does not provide authentication, authorization, rate limiting, or audit logging.
+Exposing the built-in HTTP Server directly to the public Internet is not a supported production boundary.
 For remote access, place it in a trusted network or behind a reverse proxy or gateway that handles:
 
 - TLS termination.
 - Authentication.
 - Authorization policy.
+- Rate limiting.
 - Network access control.
 - Audit logs.
 
@@ -90,7 +92,7 @@ HTTP binding recommendations:
 
 - Use `127.0.0.1` for local debugging.
 - Use a controlled network interface for container or intranet deployments.
-- Avoid exposing the MCP Server directly to remote clients.
+- Avoid exposing the MCP Server directly to remote clients. Route remote access through a trusted gateway.
 - When sessions must be associated with external users or request sources, let a trusted gateway inject session attribution headers. Do not allow clients to forge these headers directly.
 
 ### Trusted gateway and TLS termination example
